{% extends 'base.html.twig' %}

{% block title %}| Civ 6 > Leaders{% endblock %}

{% block body %}
  <ul>
    {% for name in names %}
      <li><a href="{{ path(app.request.get('_route')) }}/{{ name }}">{{ name }}</a></li>
    {% endfor %}
  </ul>
{% endblock %}
